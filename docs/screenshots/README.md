# Screenshots

Drop the demo images referenced by the main [`README.md`](../../README.md) into this
folder. The README already links to the file names below, so the moment you add an
image with the matching name it will render automatically (on GitHub and in most
Markdown viewers).

Recommended captures (PNG, ~1280px wide):

| File name              | What to capture                                                        |
| ---------------------- | ---------------------------------------------------------------------- |
| `sign-in.png`          | The sign-in screen (volunteer/admin toggle)                            |
| `signup.png`           | The multi-step sign-up flow                                            |
| `volunteer-profile.png`| A volunteer's profile with skills, availability and upcoming events    |
| `volunteer-events.png` | The volunteer-facing events browser / event sign-up page               |
| `admin-dashboard.png`  | The org admin dashboard (stat cards + events)                          |
| `create-event.png`     | The "create event" form                                                |
| `matching.png`         | The volunteer↔event matching results with score chips                  |
| `volunteer-history.png`| The admin volunteer-history / CSV report view                          |

> Tip: keep the names exactly as above, or update the paths in `README.md` if you
> prefer different names.
